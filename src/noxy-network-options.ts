export type NoxyNetworkOptions = {
  /** Dashboard **APP_ID** */
  appId: string;
  /** Dashboard **APP_SIGNING_SECRET** */
  appSigningSecret: string;
  /** HTTPS relay URL, e.g. https://relay.noxy.network */
  relayUrl: string;
};
